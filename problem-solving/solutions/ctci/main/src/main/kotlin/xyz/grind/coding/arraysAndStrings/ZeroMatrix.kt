package xyz.grind.coding.arraysAndStrings

import java.math.BigDecimal
import kotlin.math.PI

object ZeroMatrix {
    fun doubly(x: BigDecimal): BigDecimal {
        return x * PI as BigDecimal
    }
}
